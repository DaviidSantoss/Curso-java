package F01_Teste.basico;

public class A02_ORM {

    /*
     * O ORM (Mapeamento Objeto-Relacional) é um padrão de software que permite
     * converter automaticamente objetos de uma aplicação em registros de um banco
     * de dados relacional e vice-versa.
     */

    /*
     * Em outras palavras, o ORM elimina a necessidade de escrever SQL manualmente
     * para manipular dados no banco, tornando o processo mais fácil, legível e
     * integrado ao código Java.
     */

    /*
     * // O EntityManager usa ORM para converter o objeto "Usuario" em um registro
     * no banco.
     * 
     * Usuario usuario = new Usuario("João", "joao@email.com");
     * 
     * // Salva o objeto no banco de dados usando o método persist(), sem precisar
     * de SQL.
     * 
     * em.persist(usuario);
     */
}
